package com.squad1.hackathon.repository;

import com.squad1.hackathon.entity.AccountAccountType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountAccountTypeRepository extends JpaRepository<AccountAccountType, Integer> {
}